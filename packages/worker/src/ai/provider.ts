/**
 * AIProvider interface re-export from @coderev/shared.
 * Also exports the provider factory.
 */
export type {
  AIProvider,
  ReviewContext,
  ReviewResult,
  Finding,
  ValidationError,
} from '@coderev/shared';
